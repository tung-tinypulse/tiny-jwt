require "spec_helper"

describe Tiny::Jwt do
  it "has a version number" do
    expect(Tiny::Jwt::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
